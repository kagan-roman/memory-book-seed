# Оглавление wiki

База знаний (паттерн llm-wiki Карпаты). Заполняется по ходу конвейера —
`/ingest` создаёт стабы людей, `/wiki` переносит стабилизированные факты.
Не переисследуй то, что уже здесь.

## Люди (`people/`)

_(пусто — появится после `/ingest`)_

## Места (`places/`)

## Воинские части (`units/`)

## События и операции (`events/`)

## Семьи и ветки (`families/`)

## Главы книги (`../book/chapters/`)

_(заполняет `wiki-curator` в порядке `book/_master/outline.md`)_
