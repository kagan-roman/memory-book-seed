---
description: Написание глав из собранного материала
argument-hint: "[slug или пусто = все]"
---

Стадия **write**. Прочитай `book.config.yml`, `CLAUDE.md` (§2 целиком),
`book/_master/STYLE-RULES.md`, `book/_master/outline.md`.

Для каждого человека с готовыми `facts.md`+`context.md` (или для
`$ARGUMENTS`) запусти агента `chronicler` → `book/chapters/<slug>/draft.md`
(ОДИН файл, версии = git).

- Батчами по `run.parallel_batch`, по одному человеку на агента.
- Тон Алексиевич/Гранин; ноль вымысла; зачин-канон §17 (маркер `<!-- tree -->`
  для мини-дерева); эпитеты §16; без повторов с введением/врезками §4.
- Объём — по материалу, пустоту не растягивать (мало данных → короткая
  честная глава, не вода).

После прохода — сводка: объёмы глав, где видны пробелы материала (вернуть в
`/research`), что готово к `/review`. Не пересобирать PDF (§13).
